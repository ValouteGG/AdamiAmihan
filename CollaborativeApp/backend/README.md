# Backend (placeholder)

This folder is reserved for backend code. It contains a minimal Express server placeholder. To run locally:

```bash
cd backend
npm install
node server.js
```

Add your API routes and database integration here.
